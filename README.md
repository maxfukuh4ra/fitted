# Fitted

## Frontend:

To start frontend run:
`cd client`
`npm install`
`npx expo start`

## Backend:
To run background:

`cd server`
`npm install`
`npm run dev`

Make sure to add you .env keys for:
 - SUPABASE_URL
 - SUPABASE_SERVICE_KEY